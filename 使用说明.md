# 四六级自动抢报工具 · 使用说明

> ⚠️ **温馨提示**：本软件仅供学习娱乐，请遵守报名网站规则、学校规定及相关法律法规，请勿用于任何违法用途。使用本工具产生的一切后果由使用者自行承担。

---

## 目录

1. [软件能做什么](#1-软件能做什么)
2. [启动前准备](#2-启动前准备)
3. [快速上手（5 步）](#3-快速上手5-步)
4. [界面总览](#4-界面总览)
5. [账号管理](#5-账号管理)
6. [设置页怎么填](#6-设置页怎么填)
7. [启动与批量操作](#7-启动与批量操作)
8. [定时抢报（定时查询 / 定时确认）](#8-定时抢报定时查询--定时确认)
9. [右键「测试」窗口](#9-右键测试窗口)
10. [验证码识别](#10-验证码识别)
11. [它会自动做哪些事](#11-它会自动做哪些事)
12. [文件保存在哪 / 如何升级](#12-文件保存在哪--如何升级)
13. [常见问题](#13-常见问题)

---

## 1. 软件能做什么

- 同时管理**多个账号**，每个账号开一个独立的 Chrome 浏览器窗口，互不串号；
- **自动登录**：自动填账号密码、自动识别并填写验证码，识别错了会自动刷新重试；
- 登录后**安静值守**：掉线了会自动重新登录；
- **定点抢报**：设定开抢时间，到点自动点击查询；进入报名页面后自动勾选协议、自动识别验证码并提交；
- 验证码识别有**免费的本地识别**（离线、不花钱），也支持云端识别服务；
- 所有账号和设置只存在你自己的电脑上。

---

## 2. 启动前准备

1. **找一个固定的普通文件夹放程序**，例如 `D:\四六级抢报\`，把 `四六级自动抢报.exe` 放进去。
   - ❌ 不要放在 `C:\Program Files` 这类需要管理员权限的文件夹，否则设置无法保存；
   - ❌ 不建议放在压缩包里直接双击运行，请先解压出来。
2. 电脑需安装 **Google Chrome 浏览器**（官方正式版即可，程序会自动调用它，驱动也会自动下载）。
3. **双击 `四六级自动抢报.exe`** 启动。
   - 首次启动可能需要等 10～30 秒，属正常现象；
   - 若出现 Windows 蓝色提示窗口（SmartScreen），点「**更多信息 → 仍要运行**」；
   - 若杀毒软件报警拦截，请将本程序加入信任（单文件程序自解压时容易被误报）。

---

## 3. 快速上手（5 步）

1. **添加账号**：左侧「账号管理」点 **➕ 添加账号**，填用户名（学号/身份证号）、密码、省份。
2. **填写设置**：切到右侧 **⚙ 设置**：
   - 填 **目标登录网址**；
   - **识别服务** 选 `local`（免费离线，推荐大多数人用这个）；
   - 其他选项可先不填。
3. 点页面下方 **💾 保存设置**，看到「设置已保存」提示。
4. **启动**：选中账号，点上方 **▶ 启动选中**（多个账号就点 **▶ 启动全部**）。
5. 看到日志出现「**✅ 登录成功，进入后台静默监测**」就说明已经在值守了。

需要定点开抢：右键账号 →「**⏰ 定时设置1（定时查询）**」→ 填入开抢时间即可（详见第 8 节）。

---

## 4. 界面总览

```
┌─────────────────────────────────────────────────────────┐
│ 顶部：启动全部 / 启动选中 / 停止全部 / 探测CSS 等按钮      │
├──────────────────────┬──────────────────────────────────┤
│  账号管理（左侧）      │  右侧选项卡：                      │
│  账号列表表格          │   ├ ⚙ 设置                        │
│  添加/编辑/删除        │   └ 📋 日志（全局 + 每个账号一页）   │
│  导入/粘贴导入/导出    │                                  │
│  全选 / 窗口排列       │                                  │
└──────────────────────┴──────────────────────────────────┘
```

- 账号列表显示：**账号 / 省份 / 代理 / 状态**，其中「状态」会实时反映该浏览器的运行情况。
- 底部状态栏会提示当前正在做什么。
- 日志页可以看到每一步操作，遇到问题先看日志提示。

---

## 5. 账号管理

### 5.1 单个添加 / 修改

| 按钮 | 作用 |
|---|---|
| ➕ 添加账号 | 用户名、密码、省份必填；代理、备注可不填 |
| ✏ 编辑 | 修改选中的账号 |
| 🗑 删除 | 删除选中账号（可多选） |
| ☑ 全选 / ⬜ 取消选择 | 批量勾选账号 |

### 5.2 批量导入（📋 导入）

适合账号多的情况，支持 **Excel（.xlsx）** 和**文本文件（.txt / .csv）**：

- Excel：第一列用户名、第二列密码、第三列省份；
- 文本文件每行一个账号，分隔符可以是英文逗号 `,`、短横线 `----`、Tab 或竖线 `|`。

示例：

```
123456789012345678,mypassword123,北京
123456789012345679----mypassword456----上海
# 以 # 开头的行会被忽略
```

### 5.3 📝 粘贴导入

不想建文件时，点此按钮直接把多行账号文字粘贴进窗口，格式和上面文本相同。

### 5.4 💾 导出

把当前全部账号导出成一个文件备份。**建议账号配置好后导出一份存好**，换电脑或重装时可直接导回。

> 🔒 账号信息保存在你本机的程序文件夹里，请妥善保管，不要把账号文件发给别人。

---

## 6. 设置页怎么填

切到右侧 **⚙ 设置** 页。**每次修改完记得点「💾 保存设置」。**

### 6.1 基本设置

| 项目 | 怎么填 |
|---|---|
| 目标登录网址 | 报名/登录页面的完整网址，浏览器打开后会自动进入此页面 |
| 省份下拉框 | 一般不用改。需要调整时点「🎯 拾取」在浏览器里点一下省份下拉框即可自动填入 |
| 省份值 | 填要选择的省份名称，如 `河南省` |

### 6.2 表单元素 CSS 选择器

用户名框、密码框、验证码图片、验证码输入框、登录按钮的定位信息。**默认值一般都能用，不要随意改**。
只有当页面改版、账号密码填不进去时，先启动浏览器打开页面，再点对应项目旁边的「🎯 拾取」，在网页上点一下目标元素即可。

### 6.3 验证码识别服务

| 项目 | 说明 |
|---|---|
| 识别服务 | `local` = 本地模型（免费、离线，推荐）；`capmonster`、`2captcha` = 云端付费识别 |
| API Key | 选云端服务时，填你在对应平台的 Key；旁边「查询余额」可查剩余额度 |
| 本地模型路径 / 字符集路径 | **留空即可**，程序已内置模型，不需要自己选 |
| 最低置信度 | 识别结果可信度低于此数值就丢弃重试。`0.00` = 不限制。老识别错可适当调高（如 0.5） |

### 6.4 浏览器设置

- **无头模式**：勾选后浏览器窗口隐藏，在后台运行。**刚开始使用时不要勾**，等流程跑顺了再考虑；
- **全局代理**：需要代理时填，如 `http://127.0.0.1:7890`，不用就留空。单个账号也可以在编辑账号时单独填代理。

---

## 7. 启动与批量操作

### 顶部按钮

- **▶ 启动全部 / 启动选中**：开始自动登录并值守；
- **■ 停止全部**：关闭所有浏览器；
- **🔍 探测CSS**：打开一个专用浏览器，帮你分析页面元素（一般用不到）；
- **刷新状态 / 清空日志**：辅助按钮；
- **🪟 窗口排列**：浏览器开了多个时，可一键「平铺铺满屏幕 / 全部最大化 / 层叠排列」，方便同时查看。

### 右键菜单（在账号上点鼠标右键）

- 选中**一个**账号：启动、⏸ 暂停、▶ 恢复、🔄 重新启动、关闭、🔍 测试、⏰ 定时设置、编辑、删除；
- 选中**多个**账号：批量启动、批量暂停、批量恢复、批量重启、批量关闭。

说明：

- **暂停** = 浏览器保留，但停止自动操作；**恢复** = 继续；
- **重新启动** = 关闭该账号浏览器并重新登录。

---

## 8. 定时抢报（定时查询 / 定时确认）

右键账号可见两个定时选项：

| 选项 | 作用 |
|---|---|
| ⏰ 定时设置1（定时查询） | 到设定时间自动点击查询按钮 |
| ⏰ 定时设置2（定时确认） | 到设定时间自动执行确认操作 |

**时间格式：`年:月:日:时:分:秒`**

例如想在 2026 年 9 月 19 日 14:30:00 开抢，就输入：

```
2026:09:19:14:30:00
```

小技巧：

- 输入框会**自动补冒号**，直接连续敲数字即可；
- 设置成功后，该账号的浏览器悬浮窗会显示**倒计时**；
- 不设定时间时，程序按固定间隔自动查询；
- 想取消定时：打开定时窗口，把时间删空后保存。

> ⏰ 程序按**电脑系统时间**执行，开抢前务必校准电脑时间！建议提前 2～3 分钟启动账号，确认已登录并停留在正确页面。

---

## 9. 右键「测试」窗口

右键某个账号 →「🔍 测试」，这是排查问题用的工具箱：

- **当前浏览器网址**：查看该账号浏览器当前页面；
- **CSS 控件检测**：检查用户名框、密码框、验证码等元素是否能在页面找到；
- **报名提交环节测试**：
  - 「测试移除 disabled+勾选」：手动测试勾选协议复选框；
  - 「测试取图+识别+填入」：抓一张验证码、识别、填进去，验证识别是否正常；
- **自定义 CSS 检测 / 拾取 / 点击**：高级排错用，普通用户一般用不到。

---

## 10. 验证码识别

- **本地识别（local，推荐先用）**：模型已内置在程序中，**不联网、不花钱、不限次**。遇到不认识的验证码会自动换一张重试；
- **云端识别（capmonster / 2captcha）**：识别种类更多，按次收费，需要先在平台注册充值、填入 API Key，且电脑网络能正常访问该平台。可点「查询余额」确认 Key 是否有效；
- 识别老是错误时：先试试把「最低置信度」调成 `0.00`，再逐步找到合适的值（一般 0.3～0.5）；或临时改用云端识别。

---

## 11. 它会自动做哪些事

启动一个账号后，程序按下面流程自动运行，你只需观察日志：

1. 打开登录页 → 自动填账号密码 → 识别验证码并填入 → 点登录；
2. 验证码错误会自动刷新重试；
3. 登录成功后进入**安静值守**状态（不频繁操作，避免触发风控），并持续检测登录状态，一旦掉线立刻自动重登；
4. **查询阶段**：设了定时就等时间到（悬浮窗显示倒计时），到点自动点查询；没设定时就按固定间隔查询；
5. **报名提交阶段**：当页面出现查询按钮和验证码图片时，自动勾选协议复选框，并循环「取验证码 → 识别 → 填入 → 提交」，直到页面跳转完成报名。

---

## 12. 文件保存在哪 / 如何升级

程序产生的所有文件都在 **exe 所在的同一个文件夹**里：

| 文件/文件夹 | 内容 | 说明 |
|---|---|---|
| `accounts.json` | 你的全部账号 | **重要，注意备份** |
| `settings.json` | 你的全部设置 | 保留它升级后不用重新设置 |
| `browser_profiles\` | 各账号浏览器数据 | 保留有助于减少重复验证 |
| `webdriver_cache\` | 浏览器驱动缓存 | 可删除，会自动重新下载 |

**升级软件非常简单：用新的 `四六级自动抢报.exe` 覆盖旧的即可**，账号和设置原样保留，不会丢失。

建议：

1. 定期用「💾 导出」备份账号；
2. 升级前可以把 `accounts.json`、`settings.json` 复制一份到别处；
3. 整个程序文件夹可以直接复制到其他电脑使用。

---

## 13. 常见问题

**Q：设置保存后，重新打开又没了？**
请确认：① 使用的是最新版程序；② exe 放在普通文件夹（如 `D:\四六级抢报\`），不要放在「Program Files」或压缩包里；③ 保存后 exe 同目录会出现/更新 `settings.json` 文件，修改时间为当前时间。

**Q：导入的账号重启后消失？**
同上。也可以导入后先点「💾 导出」验证账号确实保存成功。

**Q：双击 exe 没反应？**
首次启动较慢，请等待 30 秒；检查杀毒软件是否拦截并添加信任；打开任务管理器确认没有残留的程序进程。

**Q：浏览器打不开 / 提示 Chrome 相关错误？**
请先安装官方 Google Chrome；可删除程序文件夹里的 `webdriver_cache` 后重试（会自动重新下载驱动）；使用代理的用户检查代理是否可用。

**Q：验证码识别不准，一直刷新？**
识别服务选 `local`；把「最低置信度」先调成 `0.00` 试能否提交，再找合适数值；实在识别不出可改用云端服务。也可以用右键 → 测试 →「测试取图+识别+填入」单独验证。

**Q：登录成功后怎么看不到它在操作？**
这是正常设计。登录成功后程序安静监测、掉线才重登，到查询时间或进入报名页面才会快速动作，目的是降低被网站风控的概率。

**Q：到了时间没有自动查询？**
检查：电脑时间是否准确、定时格式是否为 `2026:09:19:14:30:00`、账号是否处于运行状态（非暂停）、是否停留在正确页面，并看悬浮窗倒计时和该账号日志。

**Q：一次开多少账号合适？**
账号之间相互独立、不会串号，但每个浏览器都占用电脑内存，普通电脑建议同时不超过 5 个。

**Q：我的账号密码安全吗？**
账号和设置只保存在你自己电脑的程序文件夹中；本地验证码识别完全离线运行，不会联网。仅当你主动选择云端识别服务时，验证码图片才会发送到对应的识别平台。

---

*如界面文字与本说明略有差异，以程序实际界面为准。祝你报名顺利！*
